# Dataset Placeholder

Place `KDDTrain+.txt` and `KDDTest+.txt` here before running the pipeline.

The raw NSL-KDD text files are intentionally ignored by Git because they are dataset artifacts, not source code.
